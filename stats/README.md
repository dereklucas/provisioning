This is a Virtualbox VM with Ansible roles set up for:

* Base Ubuntu 14.04 install
* nginx
* Graphite & statsd

After running `vagrant up` there should be a Graphite instance
accessible at `http://stats.local`.
